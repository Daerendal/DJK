@page
@model LoginWith2faModel
@{
    ViewData["Title"] = "Uwierzytelnianie dwuskładnikowe";
}


<h1>@ViewData["Title"]</h1>
<hr />
<div class="row  offset-1">
<p>Twój login jest chroniony przez aplikację uwierzytelniającą. Wprowadź poniżej swój kod uwierzytelniający.</p>

    <div class="col-md-4">
        <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
            <input asp-for="RememberMe" type="hidden" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-floating">
                <input asp-for="Input.TwoFactorCode" class="form-control" autocomplete="off" />
                <label asp-for="Input.TwoFactorCode" class="form-label">Kod autoryzujący</label>
                <span asp-validation-for="Input.TwoFactorCode" class="text-danger"></span>
            </div>
            <div>
                <div class="checkbox">
                    <label asp-for="Input.RememberMachine" class="form-label">
                        <input asp-for="Input.RememberMachine" />
                        Zapamiętaj urządzenie
                    </label>
                </div>
            </div>
            <div>
                <button type="submit" class="w-100 btn btn-lg btn-primary">Zaloguj</button>
            </div>
        </form>
    </div>


<p>
    Nie masz dostępu do swojego urządzenia uwierzytelniającego? Możesz
    <a id="recovery-code-login" asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">zalogować się za pomocą kodu odzyskiwania</a>.
</p>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}